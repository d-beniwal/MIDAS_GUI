"""Small, temporary vendored copies of not-yet-released upstream MIDAS code
that midas-gui wants to ship ahead of an official package release.

Each subpackage documents its own provenance and removal criteria in its
own ``__init__.py``. Currently empty — the last occupant
(``frozen_point_calib``) was removed once its pipeline shipped in
``midas_calibrate_v2``.
"""
